<%= turbo_frame_tag kpt_board do %>
  <%= link_to edit_kpt_board_path(kpt_board), data: { turbo_frame: :_top }, class: 'btn btn-link d-block text-decoration-none text-start px-0' do %>
    <div class="card">
      <div class="card-body d-flex justify-content-between align-items-center">
        <p class="card-text mb-0">
          <%= "#{l(kpt_board.start_on)} 〜 #{l(kpt_board.end_on)}" %>
        </p>
      </div>
    </div>
  <% end %>
<% end %>
