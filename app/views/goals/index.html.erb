<div class="vstack gap-3">
  <%= render 'application/title', title: '目標一覧' %>
  <div>
    <%= link_to '追加', new_goal_path, class: 'btn btn-primary' %>
  </div>
  <%= turbo_frame_tag :goals, class: 'vstack gap-2' do %>
    <%= render partial: 'goal', collection: @goals %>
  <% end %>
</div>
